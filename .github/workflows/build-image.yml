name: Docker Image CI

on:
  push:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run:  cd docker && docker build . --file Dockerfile --tag habitat-cuda10.1-cudnn7
